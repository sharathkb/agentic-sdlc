# Core (always required)
pydantic>=2.6
pydantic-settings>=2.2

# Live mode only (real Anthropic API). Omit to run fully offline in mock mode.
anthropic>=0.40

# Required to run the GENERATED url-shortener app + its tests
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27

# Testing
pytest>=8.0
